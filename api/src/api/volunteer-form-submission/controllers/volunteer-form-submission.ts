/**
 * volunteer-form-submission controller
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreController('api::volunteer-form-submission.volunteer-form-submission');
